import { ActionTypes } from '../registry'

const initialState = {
  test: '123'
}

export default function app(state = initialState, action) {
  switch (action.type) {

  default:
    return state

  }
}
